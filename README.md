# custom-crm-nestJs
a ready-to-run, large‑scale CRM starter using a modern, production‑minded stack (TypeScript + NestJS + PostgreSQL + Prisma + Kafka via Redpanda + OpenSearch + Redis + Docker). Do docker compose up to get services and dependencies running locally, with a seeded admin user and end‑to‑end events flowing into search.
